# packages/markitdown/tests/_test_vectors.py

**[← Back to Parent Directory](./README.md)**

---

<details>
<summary>📊 Documentation Metadata (click to expand)</summary>

```json
{
  "source_file": "packages/markitdown/tests/_test_vectors.py",
  "file_hash": "f675d6d404617e1e1689184e44da357b529068183e076c753bbc1f3b4aab9a6f",
  "last_updated": "2026-01-04T17:23:25.065430+00:00",
  "functions": {
    "FileTestVector": {
      "hash": "291f380edfe87d555cd728afae9ac7101e243e43d9b20121bde4af77ca6e6951",
      "lines": "6-14",
      "last_updated": "2026-01-04T17:23:25.065370+00:00"
    }
  }
}
```

</details>



The Python file `packages/markitdown/tests/_test_vectors.py` implements a data structure for defining test vectors related to various file types. It includes a single class, `FileTestVector`, which is a frozen dataclass that encapsulates attributes necessary for testing file content. Each instance of `FileTestVector` contains fields such as `filename`, `mimetype`, `charset`, `url`, `must_include`, and `must_not_include`, which specify the expected characteristics and content requirements for different file types.

The file defines a collection of test vectors in the `GENERAL_TEST_VECTORS` list, which consists of multiple `FileTestVector` instances. Each instance is initialized with specific values for its attributes, representing various file formats such as DOCX, XLSX, PPTX, PDF, HTML, CSV, JSON, XML, and ZIP. The `must_include` and `must_not_include` lists within each vector specify strings that must be present or absent in the corresponding file content, respectively.

The code imports the `dataclasses` module and the `List` type from the `typing` module. These imports are used to define the `FileTestVector` class and its attributes. The file does not interact with external services or APIs, nor does it define any functions beyond the class definition. The primary data structure manipulated in this file is the `FileTestVector` dataclass, which organizes test data for use in file content validation.

## Function Relationships

<details>
<summary><strong>View Function Relationships</strong></summary>

This section documents nested functions and dependencies to provide clarity about the code structure and workflow.

### `FileTestVector`

<details>
<summary><strong>Calls/Dependencies</strong> (1 unique function)</summary>

- `FileTestVector`

</details>

</details>



## Functions and Classes

## `FileTestVector`

**Location:** [`packages/markitdown/tests/_test_vectors.py:6`](/packages/markitdown/tests/_test_vectors.py#L6-L14)

**Dependencies:** `FileTestVector`  


# FileTestVector Documentation

## Overview
`FileTestVector` is a class that represents a test vector for files, encapsulating metadata about a file including its name, MIME type, character set, URL, and lists of strings that must or must not be included in the file content.

## Class Attributes

### `filename`
- **Type**: `str`
- **Description**: The name of the file being represented by the test vector.

### `mimetype`
- **Type**: `str | None`
- **Description**: The MIME type of the file. This attribute can be `None`, indicating that no MIME type is specified.

### `charset`
- **Type**: `str | None`
- **Description**: The character set of the file. This attribute can be `None`, indicating that no character set is specified.

### `url`
- **Type**: `str | None`
- **Description**: The URL associated with the file. This attribute can be `None`, indicating that no URL is specified.

### `must_include`
- **Type**: `List[str]`
- **Description**: A list of strings that must be included in the content of the file.

### `must_not_include`
- **Type**: `List[str]`
- **Description**: A list of strings that must not be included in the content of the file.

## Return Value
The class does not have a return value as it is a class definition. Instances of `FileTestVector` can be created, which will contain the attributes defined above.

## Dependencies
The code does not explicitly import any external modules, APIs, or services. It relies solely on built-in Python types such as `str` and `List`.

## Usage Example
To create an instance of `FileTestVector`, the following pattern can be used:

```python
from typing import List

# Create an instance of FileTestVector
test_vector = FileTestVector()
test_vector.filename = "example.txt"
test_vector.mimetype = "text/plain"
test_vector.charset = "UTF-8"
test_vector.url = "http://example.com/example.txt"
test_vector.must_include = ["Hello", "World"]
test_vector.must_not_include = ["Goodbye"]
```

In this example, an instance of `FileTestVector` is created with specific attributes assigned to it.

---


---
*This documentation was automatically generated by AI (Woden DocBot) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
