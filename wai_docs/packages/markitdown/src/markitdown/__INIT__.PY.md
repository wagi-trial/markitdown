# packages/markitdown/src/markitdown/__init__.py

**[← Back to Parent Directory](./README.md)**

---

<details>
<summary>📊 Documentation Metadata (click to expand)</summary>

```json
{
  "source_file": "packages/markitdown/src/markitdown/__init__.py",
  "file_hash": "ba28afb90c25691ee46a7868b565369b04e5073484fc1e224d7ead4d8ed9f533",
  "last_updated": "2026-01-04T17:16:03.648154+00:00",
  "functions": {}
}
```

</details>



The `__init__.py` file in the `markitdown` package serves as an initialization module that imports and exposes various components essential for the functionality of the package. It imports the version information from the `__about__` module and several classes and constants from other modules within the package. The primary operations implemented in this file include the aggregation of key classes and exceptions that facilitate document conversion and error handling.

The file explicitly imports the following classes and constants: `MarkItDown`, `DocumentConverter`, `DocumentConverterResult`, and `StreamInfo`. The `MarkItDown` class is likely responsible for the main functionality of converting documents, while the `DocumentConverter` and `DocumentConverterResult` classes handle the conversion process and the results of those conversions, respectively. The constants `PRIORITY_SPECIFIC_FILE_FORMAT` and `PRIORITY_GENERIC_FILE_FORMAT` are also imported, suggesting they define the priority levels for different file formats during conversion. Additionally, the file imports several exception classes: `MarkItDownException`, `MissingDependencyException`, `FailedConversionAttempt`, `FileConversionException`, and `UnsupportedFormatException`, which are used for error handling during the document conversion process.

The `__all__` list defines the public API of the module, explicitly stating which classes, exceptions, and constants are intended for external use. This includes the version string, the `MarkItDown` class, the `DocumentConverter` and `DocumentConverterResult` classes, the `StreamInfo` class, and the various exception classes. The file does not define any new data structures or types but instead manipulates and exposes existing ones from the imported modules. The code relies on internal modules of the `markitdown` package, specifically `__about__`, `_markitdown`, `_base_converter`, `_stream_info`, and `_exceptions`.

## Functions and Classes



---
*This documentation was automatically generated by AI (Woden DocBot) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
