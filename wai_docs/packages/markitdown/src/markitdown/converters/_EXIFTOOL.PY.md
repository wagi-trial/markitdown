# packages/markitdown/src/markitdown/converters/_exiftool.py

**[← Back to Parent Directory](./README.md)**

---

<details>
<summary>📊 Documentation Metadata (click to expand)</summary>

```json
{
  "source_file": "packages/markitdown/src/markitdown/converters/_exiftool.py",
  "file_hash": "5f43a283a31e072a33872734f8b82e82174c834d2f3720e65b7036c21a6665eb",
  "last_updated": "2026-01-04T17:20:20.684114+00:00",
  "functions": {
    "_parse_version": {
      "hash": "b12efa8a0b1b45ac7584dcdf609a66548d9103ddf3b79354de2872b0c7671390",
      "lines": "7-10",
      "last_updated": "2026-01-04T17:20:20.684053+00:00"
    }
  }
}
```

</details>



The Python file `_exiftool.py` implements functionality for interacting with the ExifTool command-line application to extract metadata from files. It contains a function `_parse_version` that converts a version string into a tuple of integers for comparison purposes. The primary function, `exiftool_metadata`, takes a binary file stream and an optional path to the ExifTool executable. It verifies the version of ExifTool to ensure it meets a minimum requirement and then runs ExifTool to extract metadata in JSON format from the provided file stream.

The file defines two functions: `_parse_version` and `exiftool_metadata`. The `_parse_version` function takes a version string as input and returns a tuple of integers derived from the version components. The `exiftool_metadata` function checks if the `exiftool_path` is provided, verifies the ExifTool version, and if valid, executes ExifTool to read metadata from the given file stream. It handles exceptions related to subprocess execution and version parsing, raising appropriate runtime errors when necessary.

The code imports several modules: `json` for handling JSON data, `locale` for determining the preferred encoding, and `subprocess` for executing external commands. The function `exiftool_metadata` manipulates a `BinaryIO` file stream and returns data of type `Any`, which is intended to be JSON data parsed from the ExifTool output. The code also utilizes tuples for version comparison and handles string data for command execution and output processing.

## Function Relationships

<details>
<summary><strong>View Function Relationships</strong></summary>

This section documents nested functions and dependencies to provide clarity about the code structure and workflow.

### `_parse_version`

<details>
<summary><strong>Calls/Dependencies</strong> (4 unique functions)</summary>

- `_parse_version`
- `map`
- `split`
- `tuple`

</details>

</details>



## Functions and Classes

## `_parse_version`

**Location:** [`packages/markitdown/src/markitdown/converters/_exiftool.py:7`](/packages/markitdown/src/markitdown/converters/_exiftool.py#L7-L10)

**Dependencies:** `_parse_version`, `map`, `split`, `tuple`  


# Function Documentation: _parse_version

## Description
The `_parse_version` function takes a version string formatted as "X.Y.Z" (where X, Y, and Z are integers) and parses it into a tuple of integers. The function splits the input string by the period (.) character and converts each resulting substring into an integer.

## Parameters
- **version** (str): A string representing the version number. The string must be formatted such that it can be split into components by the period (.) character. Each component must be convertible to an integer.

## Return Value
- Returns a tuple of integers. The tuple contains the integer representations of the components of the version string. For example, if the input is "1.2.3", the output will be (1, 2, 3).

## Dependencies
The function does not have any dependencies on external modules, APIs, or services. It uses built-in Python functions such as `str.split()` and `map()`.

## Usage Example
```python
version_tuple = _parse_version("1.2.3")
# version_tuple will be (1, 2, 3)
```

---


---
*This documentation was automatically generated by AI (Woden DocBot) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
