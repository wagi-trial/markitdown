# packages/markitdown/src/markitdown/converters/_llm_caption.py

**[← Back to Parent Directory](./README.md)**

---

<details>
<summary>📊 Documentation Metadata (click to expand)</summary>

```json
{
  "source_file": "packages/markitdown/src/markitdown/converters/_llm_caption.py",
  "file_hash": "1f59673a3214b0f02bb669b6c883166fd39f9b6fbcb0e3f777d1eb184e92b4bc",
  "last_updated": "2026-01-04T17:21:02.788720+00:00",
  "functions": {}
}
```

</details>



The Python file `packages/markitdown/src/markitdown/converters/_llm_caption.py` implements a function named `llm_caption`, which generates a caption for an image by utilizing the OpenAI API. The function takes a binary file stream of the image, along with a `StreamInfo` object that provides metadata about the image, and parameters for the API client, model, and an optional prompt. If the prompt is not provided, a default prompt is used to request a detailed caption for the image.

The `llm_caption` function performs several operations: it retrieves the MIME type of the image from the `StreamInfo` object or guesses it based on the file extension, encodes the image data in base64 format, constructs a data URI for the image, prepares a message structure for the OpenAI API request, and sends the request to the API. The function returns the generated caption from the API response or `None` if an error occurs during processing.

The file imports the `BinaryIO` and `Union` types from the `typing` module, the `base64` module for encoding, and the `mimetypes` module for determining MIME types. It also imports the `StreamInfo` class from a sibling module. The `llm_caption` function manipulates the binary data of the image, the MIME type as a string, and the structured message format required by the OpenAI API, which consists of a list of message dictionaries containing roles and content types.

## Functions and Classes



---
*This documentation was automatically generated by AI (Woden DocBot) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
