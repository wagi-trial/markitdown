# packages/markitdown/src/markitdown/converters/_transcribe_audio.py

**[← Back to Parent Directory](./README.md)**

---

<details>
<summary>📊 Documentation Metadata (click to expand)</summary>

```json
{
  "source_file": "packages/markitdown/src/markitdown/converters/_transcribe_audio.py",
  "file_hash": "e43b9f6d39f33648d52f81c3e0c71649490212d1823293765e6f3f874b698a3c",
  "last_updated": "2026-01-04T17:22:26.923302+00:00",
  "functions": {
    "transcribe_audio": {
      "hash": "7195160bc997ad3955ef3d71168cc60ba246ec8612ac8f3df763b667cb76de08",
      "lines": "23-50",
      "last_updated": "2026-01-04T17:22:26.923242+00:00"
    }
  }
}
```

</details>



The Python file `packages/markitdown/src/markitdown/converters/_transcribe_audio.py` implements a function for transcribing audio files into text. The primary operation is encapsulated in the `transcribe_audio` function, which takes an audio file stream and an optional audio format argument, defaulting to "wav". The function checks for the presence of required dependencies, processes the audio file based on its format, and utilizes the Google Speech Recognition API to convert the audio into text.

The `transcribe_audio` function is the only function defined in this file. It is responsible for validating the audio format, handling audio files in various formats (including "wav", "aiff", "flac", "mp3", and "mp4"), and returning the transcribed text. If the audio format is unsupported, it raises a `ValueError`. Additionally, if the required dependencies are not installed, it raises a `MissingDependencyException`, providing guidance on how to install the necessary packages.

The code explicitly imports the `speech_recognition` and `pydub` libraries, which are used for audio processing and transcription. It also imports the `io` and `sys` modules from the standard library. The function manipulates data types such as `BinaryIO` for the audio file stream and uses `io.BytesIO` to handle in-memory byte streams. The interface for the audio source is managed through the `sr.AudioFile` class from the `speech_recognition` library, which is used to read the audio data for transcription.

## Function Relationships

<details>
<summary><strong>View Function Relationships</strong></summary>

This section documents nested functions and dependencies to provide clarity about the code structure and workflow.

### `transcribe_audio`

<details>
<summary><strong>Calls/Dependencies</strong> (13 unique functions)</summary>

- `AudioFile`
- `BytesIO`
- `MissingDependencyException`
- `Recognizer`
- `ValueError`
- `export`
- `from_file`
- `recognize_google`
- `record`
- `seek`
- `strip`
- `transcribe_audio`
- `with_traceback`

</details>

</details>



## Functions and Classes

## `transcribe_audio`

**Location:** [`packages/markitdown/src/markitdown/converters/_transcribe_audio.py:23`](/packages/markitdown/src/markitdown/converters/_transcribe_audio.py#L23-L50)

**Dependencies:** `AudioFile`, `BytesIO`, `MissingDependencyException`, `Recognizer`, `ValueError`, `export`, `from_file`, `recognize_google`, `record`, `seek`, `strip`, `transcribe_audio`, `with_traceback`  


# Function Documentation: `transcribe_audio`

## Description
The `transcribe_audio` function transcribes audio content from a given file stream into text. It supports various audio formats and utilizes the Google Speech Recognition API for transcription. If no speech is detected, it returns a specific message.

## Parameters

- `file_stream` (BinaryIO): 
  - A binary stream representing the audio file to be transcribed. This stream is expected to be in one of the supported audio formats.
  
- `audio_format` (str, optional): 
  - The format of the audio file. Default is `"wav"`.
  - Accepted values are:
    - `"wav"`
    - `"aiff"`
    - `"flac"`
    - `"mp3"`
    - `"mp4"`
  - If the format is `"mp3"` or `"mp4"`, the function converts the audio to `"wav"` format for processing. If an unsupported format is provided, a `ValueError` is raised.

## Return Value
- Returns a `str`:
  - The transcribed text from the audio file.
  - If no speech is detected, it returns the string `"[No speech detected]"`.

## Dependencies
- The function requires the following external modules:
  - `pydub`: For handling audio file conversion.
  - `speech_recognition` (imported as `sr`): For recognizing speech in audio files.
- It also requires the installation of the `markitdown` package with the `[audio-transcription]` optional dependencies. If the dependencies are not installed, a `MissingDependencyException` is raised.

## Usage Example
```python
from io import BytesIO

# Assuming 'audio_file' is a binary stream of an audio file in a supported format
with open('path/to/audio/file.mp3', 'rb') as audio_file:
    transcript = transcribe_audio(audio_file, audio_format='mp3')
    print(transcript)
```

---


---
*This documentation was automatically generated by AI (Woden DocBot) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
