# .github/workflows/tests.yml

**[← Back to Parent Directory](./README.md)**

---

<details>
<summary>📊 Documentation Metadata (click to expand)</summary>

```json
{
  "source_file": ".github/workflows/tests.yml",
  "file_hash": "5cb8181fdda322b53bbb26c46caf20e70c9323c967d48c9e1fcea2b355863513",
  "last_updated": "2026-01-04T17:11:46.872927+00:00",
  "functions": {}
}
```

</details>



The file `.github/workflows/tests.yml` defines a GitHub Actions workflow named "tests" that is triggered on pull requests. The workflow consists of a single job called "tests" that runs on the latest version of Ubuntu. The job includes several steps that perform specific operations related to testing a Python project.

The steps in the job include checking out the repository using the `actions/checkout@v5` action, setting up a Python environment with versions 3.10, 3.11, and 3.12 using the `actions/setup-python@v5` action, installing the Hatch package manager via `pipx`, and finally executing tests located in the `packages/markitdown` directory using the command `hatch test`. The specific responsibilities of the actions used in this workflow are as follows: `actions/checkout@v5` retrieves the repository code, `actions/setup-python@v5` configures the Python environment, and `pipx install hatch` installs the Hatch tool for managing Python packages and environments.

The workflow does not define any custom data structures, types, or interfaces. However, it utilizes external dependencies such as GitHub Actions (for workflow execution), Python (with specified versions), and the Hatch package manager. The workflow manages the execution of tests within a specific directory of the project, but it does not define or manipulate any additional data structures within the provided code.

## Functions and Classes



---
*This documentation was automatically generated by AI (Woden DocBot) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
